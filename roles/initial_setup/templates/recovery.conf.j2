standby_mode = 'on'
primary_conninfo = 'host={{ postgresql_peer_ipaddr }} port={{ postgresql_port }} user={{ postgresql_replication_user }}'
recovery_target_timeline='latest'
restore_command = 'scp {{ postgresql_peer_ipaddr }}:{{ postgresql_archive_dir }}/%f %p'
